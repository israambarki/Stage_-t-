<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">


    <uses-permission android:name="android.permission.INTERNET" />



    <application
        android:allowBackup="true"



        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/servilinkk"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Swipeimage"
        tools:targetApi="31">
        <activity
           android:name=".accueil.AccueilActivity"
            android:exported="false"
            android:label="@string/title_activity_accueilservice"
            android:theme="@style/Theme.Swipeimage" />
        <activity
            android:name=".AccueilActivity"
            android:exported="false"
            android:label="@string/title_activity_accueil"
            android:theme="@style/Theme.Swipeimage" />
        <activity
            android:name=".View.slider.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Swipeimage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>